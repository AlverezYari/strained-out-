FROM golang:1.22-alpine AS build
WORKDIR /app
COPY backend/go.mod ./
RUN go mod download
COPY backend ./
RUN go build -o server ./cmd/server

FROM alpine:latest
WORKDIR /app
COPY --from=build /app/server ./
EXPOSE 8080
CMD ["./server"]
